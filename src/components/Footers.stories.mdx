import { Meta, Story } from '@storybook/addon-docs'

import { Footer } from './Footer'

<Meta title="General/Footer" component={Footer} />

export const Template = (args) => <Footer {...args} />

# Footer

This is a `Footer`, which concise of multiple links to social media and internal pages.

<Story name="Footer">{Template.bind({})}</Story>
